# Agent Configuration

## Chrome DevTools MCP Server
When the user asks to "launch the Chrome DevTools MCP server" or "open the MCP server", use the `chrome-devtools_new_page` tool to open a new page in the browser.
